# ============================================================================
# Gathé Finance — dépôt CODE uniquement.
# Exclus : secrets, doc, images/captures, archives, artefacts de build.
# ============================================================================

# --- Secrets / env (NE JAMAIS committer) ------------------------------------
.env
.env.*
!.env.docker.example
!.env.prod.example
infra/.env.prod
infra/letsencrypt/
**/.env
**/.env.local

# --- Doc & informations hors lancement --------------------------------------
README.md
*.docx
.~lock.*#
architecture/
conception/
assets_site_existant/
Archive_14_05_2026frontend/

# --- Images / captures (hors assets applicatifs) ----------------------------
/capture.png
/capture2.png
/capture_paysika/
/capture_tara/

# --- Outils locaux IDE / agent ----------------------------------------------
.claude/
.notebook/
.idea/
.vscode/
.DS_Store

# --- Python / Django (backend) ----------------------------------------------
__pycache__/
*.py[cod]
*.egg-info/
.venv/
backend/.venv/
backend/**/migrations/__pycache__/
backend/db.sqlite3
backend/media/
backend/staticfiles/
backend/static_collected/
*.log

# --- Node / Next.js (frontend) ----------------------------------------------
node_modules/
**/node_modules/
.next/
**/.next/
**/.turbo/
**/out/
*.tsbuildinfo
npm-debug.log*

# --- Flutter / Android (mobile) ---------------------------------------------
mobile/.dart_tool/
mobile/.flutter-plugins
mobile/.flutter-plugins-dependencies
mobile/build/
mobile/**/build/
mobile/android/.gradle/
mobile/android/local.properties
mobile/android/key.properties
mobile/**/*.apk
mobile/**/*.aab
mobile/ios/Pods/
mobile/ios/.symlinks/
mobile/.idea/

# --- Divers -----------------------------------------------------------------
*.bak
*.tmp

# Artefacts intermédiaires de scripts/build-pdfs.mjs (HTML -> PDF via Playwright)
docs/*.html

# Android signing artefacts (NE JAMAIS COMMIT)
mobile/android/keystore/
mobile/android/keystore/*.jks
mobile/android/keystore/*.keystore

# Artefacts release Play Store (AAB binaires lourds) — gardés hors git, on
# garde les .md d'audit/plan/checklist en revanche.
audit/release/
audit/release/*.aab
audit/release/*.apk

# Brouillons livre projet (PDF, captures Playwright/adb, ~90 Mo)
livre_projet/

# Captures parasites laissées à la racine pendant les sessions
/capture_rh.jpeg
/capture_*.png
/capture_*.jpeg

# APK de l'app mobile servi en download depuis la vitrine. Le fichier est
# depose manuellement sur le VPS (cf. frontend/apps/site/public/downloads/
# README.md) — pas commite (74 Mo).
frontend/apps/site/public/downloads/*.apk
frontend/apps/site/public/downloads/*.aab
