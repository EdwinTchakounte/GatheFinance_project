[pytest]
# Tests AUTONOMES de la brique Tara (sans DB ni projet complet).
# Lancer simplement :  pytest
# Dépendances : pytest, django, requests  (cf. requirements du module paiement).
python_files = test_*.py
python_classes = Test*
python_functions = test_*
testpaths = tests
addopts = -ra
