from django.apps import AppConfig


class SupportConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps_coop.support"
    label = "support"
    verbose_name = "Support membre (messagerie)"
